#!/usr/bin/env bash
# Thin shim: forwards argv to `agentic-coding workflow`, preserving the exact
# agent-facing CLI surface (skills, prompts, PLAN_REJECTED loop unchanged).
set -euo pipefail
exec agentic-coding workflow "$@"
