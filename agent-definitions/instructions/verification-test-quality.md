# Test quality verification

Review whether the changed behavior is asserted by tests: whether new or changed behavior has any covering assertion, whether an assertion would still pass if the logic were broken, and whether a test merely restates the implementation. Use focused checks for the assigned scope only; never run the complete repository test suite. Report concrete evidence only.
