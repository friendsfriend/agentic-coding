## 1. Contracts and workflow runtime

- [x] 1.1 Extend `src/workflow/contracts.ts` with backward-compatible single-question and ordered questionnaire request types, optional context, stable group/item metadata, bounded parsing, and validated grouped answer envelopes; verify `bun test test/workflow-question.test.ts` covers legacy parsing, valid questionnaire payloads, ambiguous/invalid payloads, duplicate options, and item/content bounds.
- [x] 1.2 Update `src/workflow/runtime.ts` to create grouped dialogue records atomically, answer all items through one revision-checked response set, and cancel/expire a group without changing workflow lifecycle state; verify `bun test test/workflow-question.test.ts` covers FIFO isolation, exact per-item responses, authorization, stale revisions, cancellation, expiry, reload persistence, and no partial mutation on invalid input.
- [x] 1.3 Update `src/workflow/cli.ts` and the dashboard engine/data question boundary to dispatch normalized questionnaires, wait for all item records to reach a terminal state, serialize legacy versus grouped results, and submit grouped developer actions; verify `bun test test/workflow-cli.test.ts test/workflow-question.test.ts` covers bounded waits, interruption/expiry, and ordered structured result output.

## 2. Agent-facing tool contract

- [x] 2.1 Update `agent-definitions/extensions/developer-question.ts` with the questionnaire/context schema, bounds, structured-response descriptions, and questionnaire-aware call/result rendering without exposing question content in diagnostics; add the reference-informed focused tool description plus `promptSnippet`/`promptGuidelines` when supported, with equivalent fallback text in the description; verify extension registration/schema coverage or a focused build inspection confirms legacy and questionnaire shapes, context, flat option objects, recommendation/trade-off guidance, related-only batching guidance, and structured-result guidance are exposed.
- [x] 2.2 Regenerate `src/workflow/embedded.generated.ts` from the authoritative source assets using `bun run build`, without hand-editing the generated file; verify `bun test test/workflow-assets.test.ts` confirms the embedded extension matches the updated source.

## 3. Dashboard questionnaire interaction

- [x] 3.1 Refactor `src/tui/dash/ui/DeveloperQuestionModal.tsx` into a controlled legacy/questionnaire view with compact tabs, active-item context and response status, option selection, and a focused OpenTUI textarea for multiline custom answers; verify a renderer test shows tab order/status, preserves newlines, rejects blank custom text, and keeps the active question visible at bounded terminal sizes.
- [x] 3.2 Update `src/tui/dash/App.tsx` question state and keymap handling to maintain one draft/selection per stable item, navigate tabs without submitting another item, keep textarea editing owned by OpenTUI, support plain Enter newline and Alt+Enter advance/submit, cancel the whole request, and refresh after stale/expired responses; verify focused renderer interaction tests cover typing, cursor/edit operations, tab round trips, option selection, grouped submission, cancellation, and no duplicated characters.
- [x] 3.3 Connect grouped answer submission and canonical refresh through `src/tui/dash/engine.ts` and `src/tui/dash/data.ts` while retaining one-at-a-time FIFO presentation for independent questions; verify a dashboard integration test confirms current revision is used, successful responses close/advance only after acceptance, and a stale response leaves the next canonical request intact.

## 4. Focused verification and compatibility

- [x] 4.1 Add or extend focused tests in `test/workflow-question.test.ts`, `test/workflow-cli.test.ts`, and `test/dash/` for questionnaire grouping, per-item option/custom answers, structured multiline fidelity, queue isolation, bounds, cancellation, expiry, and legacy single-question compatibility; verify the targeted files pass with `bun test test/workflow-question.test.ts test/workflow-cli.test.ts test/dash/developerQuestionModal.test.tsx` (or the implemented focused dashboard test path).
- [x] 4.2 Verify the changed TypeScript and generated assets with `bun run type-check` from `agentic-coding/`, then verify formatting, import ordering, and lint diagnostics with `bun run lint` from the same directory.
