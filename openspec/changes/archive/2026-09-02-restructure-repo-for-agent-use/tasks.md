## 1. Digest guard (must land first, green against current code)

- [x] 1.1 Add `agentic-coding/test/workflow-steps.test.ts` with a pinned-digest snapshot of every `(id, version)` pair returned by `registry.definitions()` (including the legacy/policy sets and the version-1000 set), asserting each definition digest and every entry of `stepDigests` equals a literal value committed in the test. Wiki-native definitions have no legacy version 1 and must not be invented to fit the snapshot. Verify by running `bun test test/workflow-steps.test.ts` before moving any behavior — it must pass against the current registry.
- [x] 1.2 In the same file, capture the current role behavior as a parity baseline: for every agent step of every definition id, assert `rolesForDefinition(...)` output for fusion planner counts 0 and 2–5, and assert the engine's per-step fan-out for representative snapshots (`core.verification` with empty `selectedRoles`, with `testRunStarted`, and with an explicit selection; `core.wiki` under `research` and under `openspec-full`; `fusion.plan` with 2 and 5 pinned planner routes). Verify by running `bun test test/workflow-steps.test.ts` against the current registry before behavior is moved.

## 2. Seam declaration (no behavior attached yet)

- [x] 2.1 Create `agentic-coding/src/workflow/steps/types.ts` defining `StepBehavior` with the two role hooks from design D4 — `roles(ctx: { snapshot: WorkflowSnapshot }): string[]` and `candidateRoles(ctx: { definitionId: string; fusionPlannerCount: number }): string[]`, both optional — plus their context types. Contexts must carry no `Database`, engine reference, or filesystem path (design D8). Verify with `bun run type-check`.
- [x] 2.2 Widen `StepDefinition` in `agentic-coding/src/workflow/registry.ts` with `behavior?: StepBehavior` and re-export the type. Leave `stepDigest()` byte-for-byte unchanged and add a comment beside its existing instruction-asset note stating that behavior is engine-internal and must never enter the digest. Verify by running `bun test test/workflow-steps.test.ts` — task 1.1's digest snapshot must still pass unchanged.
- [x] 2.3 Extend `registerStep` validation to reject a declared behavior whose `candidateRoles` returns a non-array, or returns an empty array for a step whose `actor` is `agent`, naming the offending step and leaving no partially registered catalog. Verify with a focused case in `test/workflow-steps.test.ts` asserting the rejection and its message.

## 3. Step behavior modules

- [x] 3.1 Create `agentic-coding/src/workflow/steps/planning.ts` owning `core.plan` (`planner`), `fusion.plan` (ordered `planner-N`, from `snapshot.routing` for `roles` and from `fusionPlannerCount` for `candidateRoles`), and `fusion.consolidate` (`consolidator`). Verify with the task 1.2 parity assertions retargeted at these hooks.
- [x] 3.2 Create `agentic-coding/src/workflow/steps/implementation.ts` owning `core.implementation` (`worker`). Verify via the parity assertions.
- [x] 3.3 Create `agentic-coding/src/workflow/steps/verification.ts` owning `core.triage` (`triage`) and `core.verification`. Define the ordered six-verifier list and the `definitionId !== "no-openspec" || role !== "openspec-verifier"` filter once as module constants, with `candidateRoles` returning the filtered full list and `roles` returning `snapshot.step.selectedRoles` or the `testRunStarted ? ["test-verifier"] : ["quality-verifier"]` fallback. Add a comment stating why this step's two hooks legitimately differ (design D4). Verify with parity assertions covering all three snapshot shapes plus the `no-openspec` filter.
- [x] 3.4 Create `agentic-coding/src/workflow/steps/wiki.ts` owning `core.wiki`, resolving `research-wiki` when the definition id is `research` and `wiki` otherwise, from one shared rule used by both hooks. Verify with parity assertions for `research`, `wiki`, `wiki-comments`, `openspec-full`, and `no-openspec`.
- [x] 3.5 Create `agentic-coding/src/workflow/steps/research.ts` owning `core.research` (`researcher`). Verify via the parity assertions.
- [x] 3.6 Create `agentic-coding/src/workflow/steps/lifecycle.ts` owning `core.archive` (`archive`) and the developer/system steps `core.plan-approval`, `core.developer-review`, `core.wiki-approval`, `core.delivery`, `core.completed`, `core.closed` as behavior entries with no role hooks. Verify with a case asserting a non-agent step with an empty behavior registers successfully.
- [x] 3.7 Create `agentic-coding/src/workflow/steps/index.ts` exporting `stepBehavior(id)`, `rolesForStep(...)`, and the assembled catalog, with a construction-time assertion that every step id referenced by a registered manifest has a catalog entry. All behaviors must be module-level constants shared by reference (design D8). Verify with a focused case asserting registry construction fails, naming the step, when a manifest references a step with no behavior entry.

## 4. Remove the duplicated role logic

- [x] 4.1 Attach each step's behavior in `agentic-coding/src/workflow/definitions.ts` by sourcing it from `steps/index.ts` in the `step()` catalog construction, leaving `INSTRUCTION_BY_STEP`, contracts, `workflowEdges`, all manifests, and `definitionVersionForPolicy` untouched. Verify by running `bun test test/workflow-steps.test.ts` — the digest snapshot from 1.1 must still pass unchanged.
- [x] 4.2 Delete `roleForStep` from `agentic-coding/src/workflow/runtime.ts` and resolve fan-out roles at its single call site (line ~2493) through `stepBehavior(...).roles({ snapshot })`. Keep `fusionPlannerRoles` exported from `runtime.ts` — `recordResearchHandoff`, `fusionDraftInputs`, and tests still import it. Verify with `bun test test/workflow-runtime.test.ts test/workflow-plan-fusion.test.ts` (unmodified imports, acting as the behavior oracle).
- [x] 4.3 Replace the body of `rolesForDefinition` in `agentic-coding/src/workflow/cli.ts` with a delegation that iterates the definition's agent steps and calls `candidateRoles`, keeping its exported name, signature, and return shape so `src/tui/dash/engine.ts:226` and `test/workflow-registry.test.ts` need no edit. Verify with `bun test test/workflow-cli.test.ts test/workflow-registry.test.ts test/workflow-dashboard.test.ts`.
- [x] 4.4 Confirm no `steps/*.ts` module imports `runtime.ts`, `definitions.ts`, or `cli.ts` (design's one-way dependency rule). Verify with `rg "from \"\\.\\./(runtime|definitions|cli)\\.ts\"" src/workflow/steps/` returning no matches, plus `bun run type-check`.

## 5. Documentation

- [x] 5.1 Add `agentic-coding/docs/workflow-architecture.md` describing the layer map (manifests and graph in `definitions.ts` → per-step behavior in `steps/` → step-agnostic mechanics in `runtime.ts` → effects → presentation), the rule that role knowledge lives only in `src/workflow/steps/`, the two role-resolution moments and why they differ, a checklist for adding a step, and an explicit list of the branches stages B, C, and D still have to move. Verify the file exists and its stage list matches design.md D7.
- [x] 5.2 Add a short "Workflow architecture" section to `AGENTS.md` pointing at that document and stating the role-ownership rule, leaving the existing Biome section unchanged. Verify the section is present and links resolve.

## 6. Change-relevant validation

- [x] 6.1 Run the focused workflow suite for the touched surface: `bun test test/workflow-steps.test.ts test/workflow-runtime.test.ts test/workflow-cli.test.ts test/workflow-registry.test.ts test/workflow-plan-fusion.test.ts test/workflow-e2e.test.ts test/workflow-dashboard.test.ts` and confirm every test passes, including the unmodified digest snapshot from 1.1.
- [x] 6.2 Run `bun run type-check`, then `bun run format` followed by `bun run lint` with zero diagnostics, then `bun run build` and confirm it regenerates `src/workflow/embedded.generated.ts` without error (never hand-edit that file).
- [x] 6.3 Run `openspec validate restructure-repo-for-agent-use --strict` and confirm it passes.
