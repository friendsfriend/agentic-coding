## 1. Research graph: add completed close gate

- [x] 1.1 In `agentic-coding/src/workflow/definitions.ts`, update the `research` manifest `steps` to `["core.research", "core.wiki", "core.wiki-approval", "core.completed", "core.closed"]` and its edges so the `core.wiki-approval` `approve` edge (keeping the `wiki.verify` effect) targets `core.completed`, add a `core.completed` `close` → `core.closed` edge, and keep the `core.wiki-approval` `comments` → `core.wiki` edge. Verify with a registry/definition test asserting the research graph has the new step order and edges and remains free of implementation/verification/archive/delivery/PR steps (extend `agentic-coding/test/workflow-registry.test.ts` or the research coverage in `agentic-coding/test/workflow-runtime.test.ts`).

## 2. Runtime: gate close-research and completed actions

- [x] 2.1 In `agentic-coding/src/workflow/runtime.ts` `actions(...)`, offer `close-research` for the research flow only when `currentStep === "core.research"`, and stop offering it at `core.wiki` and `core.wiki-approval`. Verify with a `agentic-coding/test/workflow-runtime.test.ts` case asserting `close-research` is present at `core.research` and absent at `core.wiki`/`core.wiki-approval`.
- [x] 2.2 In `runtime.ts`, ensure the `core.completed` actions branch for the research flow offers an explicit `close` action and suppresses `create-pr` (add `research` to the definition list that omits `create-pr`). Verify with a runtime test asserting the research `core.completed` step offers `close` and not `create-pr`.
- [x] 2.3 In `runtime.ts`, tighten the `close-research` handler guard so it rejects when the research workflow is not at `core.research` (only the conversational stage may close-research), and confirm the existing "close works when researcher unavailable" path still succeeds at `core.research`. Verify with a runtime test asserting a `close-research` dispatch at `core.wiki-approval` is rejected without changing state, and one at `core.research` still transitions to `core.closed`.

## 3. Directive-first wiki drafting (research handoff only)

- [x] 3.1 (superseded by task group 6 — kept for history) In `agentic-coding/src/workflow/effect-runner.ts` `assignmentFor(...)`, strengthen the `isResearchWikiStep` assignment input/objective text to direct the wiki agent to create/update exactly the directive-named concepts with the listed claims first and limit inspection to targeted corroboration of those directives (no broad rediscovery). Verify with a `agentic-coding/test/workflow-effects.test.ts` (or effect-runner assignment) test asserting the research wiki assignment contains the directive-first wording and that a non-research openspec `core.wiki` assignment does not.
- [x] 3.2 (superseded by task group 6 — kept for history) In `agent-definitions/instructions/wiki.md`, update the "Research handoff input" section to direct directive-first drafting with targeted corroboration only, keeping the wording scoped to the research-handoff case so the openspec `core.wiki` discovery behavior is unchanged. Regenerate/refresh the pinned instruction digest for `core.wiki` (run `bun run build` if the digest is embedded) and verify the assignment renderer accepts the edited asset (assignment/asset test passes).

## 4. Dashboard: standard wiki review UI for research

- [x] 4.1 In `agentic-coding/src/tui/dash/App.tsx`, remove the `data().state.definition?.id !== "research"` guards in both `openRequiredUserAction` and the auto-open `createEffect` so the `wiki-review` user action opens `openPlanReview()` directly for the research flow (same as the other flows). Verify with a `agentic-coding/test/dash/userActions.test.tsx` case asserting the research `core.wiki-approval` gate opens the wiki review popup directly instead of the generic list modal.
- [x] 4.2 In `agentic-coding/src/tui/dash/data.ts` `requiredUserActionFor(...)`, update the research `wiki-approval` branch so it no longer emits the `close-research` list item (the review popup drives approve/request-changes), and remove `close-research` advertising from the research `core.wiki` phase item. Verify with `agentic-coding/test/dash/data.test.ts` cases asserting the research `wiki-approval` action has no `close-research` item and the research `core.wiki` phase does not advertise `close-research`.

## 5. End-to-end verification

- [x] 5.1 Add or extend a research-flow test covering the full success path `core.research` → handoff → `core.wiki` → `core.wiki-approval` → approve → `core.completed` → close → `core.closed`, asserting closing is only reachable via `core.completed` after approval and that `close-research` is unavailable after handoff (extend `agentic-coding/test/workflow-e2e.test.ts` or `agentic-coding/test/workflow-runtime.test.ts`).
- [x] 5.2 Run the change-relevant test files (`workflow-registry`, `workflow-runtime`, `workflow-effects`, `dash/userActions`, `dash/data`) plus `bun run lint` and `bun run type-check` from `agentic-coding/`, and verify they pass with zero diagnostics.

## 6. Developer review: split the shared wiki role into two dedicated agents

Developer review rejected the task-3 conditional-prompt shape (one `wiki` role, one instruction file with a step/workflow-conditional "Research handoff input" section) as the wrong abstraction, since research-handoff drafting and openspec/implementation drafting are different tasks. Replace it with two dedicated roles on the existing `core.wiki` step (see design.md D4/D5); no new step id.

- [x] 6.1 Split `agent-definitions/instructions/wiki.md` into a shared base file (source-repository boundary, project/shared concept-naming scoping, update-existing-before-creating sequence, OKF authoring rules, review-comment revisions — identical for both roles) plus two new role-specific files: `wiki-openspec.md` (discovery-based "how do I decide what to document", used by role `wiki`) and `wiki-research.md` (directive-first, used by role `research-wiki`, written unconditionally since every run of this role follows a handoff). Verify with the pre-existing `agentic-coding/test/workflow-wiki-scope.test.ts` cross-file consistency checks against the trimmed `wiki.md` (kept passing by relocating the project/shared-scoping guidance into the shared base file).
- [x] 6.2 In `agentic-coding/src/workflow/definitions.ts`, pin all three wiki files (plus the protocol) under `INSTRUCTION_BY_STEP["core.wiki"]`. In `agentic-coding/src/workflow/assignment.ts` `renderAssignment`, generalize the existing per-verifier-role asset selection (`verification-${role}.md`) so a `wiki-` prefixed asset is likewise included only for the role that names it (`{ wiki: "wiki-openspec.md", "research-wiki": "wiki-research.md" }`), while the unprefixed `wiki.md` base is always included. Verify with a focused assignment-render test asserting a `wiki`-role render includes `wiki-openspec.md` and excludes `wiki-research.md`, and a `research-wiki`-role render does the reverse.
- [x] 6.3 In `agentic-coding/src/workflow/runtime.ts` `roleForStep`, return role `research-wiki` instead of `wiki` for `core.wiki` when `snapshot.definition.id === "research"`. In `agentic-coding/src/workflow/effect-runner.ts` `assignmentFor(...)`, key the directive-first assignment branch off `run.role === "research-wiki"` instead of the workflow/step conditional. Verify with the existing research-handoff assignment test (`agentic-coding/test/workflow-effects.test.ts`) asserting the launched wiki run's role is `research-wiki` and its assignment carries the directive-first wording.
- [x] 6.4 In `agentic-coding/src/workflow/cli.ts`, make `rolesForDefinition` return `research-wiki` for `core.wiki` when the definition is `research`; widen `authorizeWikiWriter` and the `wiki write` role gate to accept either `wiki` or `research-wiki` identically (same authorization, promotion restrictions, and source-repository boundary for both). In `agentic-coding/src/workflow/wiki.ts`, add `research-wiki` to the role list that forces `status: draft` and strips `verified` on write. Verify with `agentic-coding/test/workflow-wiki.test.ts`'s role-isolation test extended to cover `research-wiki` identically to `wiki`.
- [x] 6.5 Update `agent-definitions/instructions/research.md`'s stale claim that `close-research` remains available "at any active research stage" to reflect that it is unavailable after handoff (task group 2). Regenerate the pinned instruction digests (`bun run build`) and update `openspec/changes/optimize-research-flow/{proposal,design}.md` and the `research-workflow`/new `knowledge-wiki` spec deltas to describe the two-role split. Verify with `openspec validate optimize-research-flow --strict`.
- [x] 6.6 Re-run the change-relevant test files including the new/extended ones (`workflow-registry`, `workflow-runtime`, `workflow-effects`, `workflow-adapters`, `workflow-wiki`, `workflow-wiki-scope`, `workflow-wiki-gate`, `dash/userActions`, `dash/data`) plus `bun run lint` and `bun run type-check` from `agentic-coding/`, and verify they pass with zero diagnostics.
