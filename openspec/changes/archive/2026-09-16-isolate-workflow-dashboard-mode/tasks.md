## 1. Composition
- [x] 1.1 Confirm contextual launch predecessor is implemented; inventory all CLI/Herdr dash call sites and classify each dashboard command/popup as required operation or removed browsing. (Inventory + classification recorded in design.md "Inventory and classification"; Herdr's only invocation is `effect-runner.ts` running `agentic-coding dash --repo --workflow-id` in the workspace pane.)
- [x] 1.2 Add explicit dashboard-only root composition using the existing dashboard and shared services; never mount the full feature shell for dash. (`src/tui/app/DashboardRoot.tsx`; `src/tui/index.tsx` selects it for `dash` and no longer passes a dashboard target to `AppShell`.)
- [x] 1.3 Preserve explicit workflow/standalone identity resolution and bounded invalid-target errors without fallback to Home or a picker. (Target validation before any resource acquisition; `test/app/dashboardRoot.test.tsx` "a missing target repository is a bounded error, not a Home fallback".)

## 2. Restricted surface
- [x] 2.1 Remove tabs, breadcrumbs, picker, Home/Settings/feature routes and all corresponding keyboard/mouse registrations from dash mode. (Dash composes `DashboardRoot`; no shell key layer, destination handler or breadcrumb/picker component is mounted.)
- [x] 2.2 Remove standalone trace/artifact browsing callbacks; preserve required review/approval/question/credential/confirmation dialogs, inline metrics and revision-bound operational controls. (The full application's trace/artifact routes and drill-down are not mounted; the dashboard's own workflow-scoped panels and dialogs are retained per the recorded classification. Dead events modal and unused `ChangedFilesBrowser`/`TraceBrowser` removed.)
- [x] 2.3 Keep local panel focus/scroll/help/exit and modal precedence; update catalogs so both footer and complete help contain only applicable commands. (Dashboard catalog is the only publisher; footer and `?` help verified to contain dashboard commands only.)
- [x] 2.4 Preserve API/subscription and owned-versus-attached lifecycle setup, including startup failure, exit and ongoing workflow execution; remove obsolete full-shell dashboard callbacks. (`DashApp` mount, shell header plumbing and the `workflows.detail` initial route removed; the process entry's server/acquisition/ownership path is unchanged.)

## 3. Validation
- [x] 3.1 Add render/input tests proving dash has no navigation chrome, destinations or hidden global handlers; test keyboard/mouse and invalid identity behavior. (`test/app/dashboardRoot.test.tsx`)
- [x] 3.2 Test required workflow reviews, answers, credentials and action submissions with revision/capability enforcement; verify inline status/metrics remain. (Root-level review open/dismiss plus inline status/metrics assertions; existing `test/dash/*` suites cover the dialogs, revision-bound submissions and credential bridge.)
- [x] 3.3 Test owned and attached cleanup without duplicate coordinators, accidental server termination or workflow data deletion. (Dashboard root acquires no server/coordinator handle; `test/lifecycle.test.ts`, `test/backend-lifecycle.test.ts` cover release-once and ownership policy.)
- [x] 3.4 Open a real Herdr-launched dash and full app; check narrow/wide rendering, local focus, footer and `?` help; record end-to-end launch/closure evidence. (Verified by the operator in a real Herdr launch; the local pty smoke evidence in design.md covers the automated part.)
- [x] 3.5 Update CLI docs and obsolete full-shell dash expectations; run relevant tests, type-check, zero-diagnostic lint and strict OpenSpec validation.
