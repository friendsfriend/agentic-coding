## 1. Baseline and route model
- [x] 1.1 Inventory current shell/environment/observability destinations, keyboard and mouse entrypoints, feature restrictions and route-local state; record replacement routes and parity evidence. (`inventory.md`)
- [x] 1.2 Replace per-feature/single-origin assumptions in the pure route reducer with typed identities, structural parents, chronological Back and keyed view state; test multi-domain hops, repeated visits and deleted resources. (`src/tui/shared/routes.ts`, `test/app/routes.test.ts`; legacy `FeatureRouterState` callers keep working through the marked bridge until tasks 2.3/3.1 migrate them.)

## 2. Pages and chrome
- [x] 2.1 Add Home and category pages, one bounded breadcrumb row and a shared location picker using existing list/modal primitives. (`src/tui/shared/navigation/` — moved there because the layer matrix forbids a feature module importing the shell (`test/workflow-source-layer-boundaries.test.ts`) — `test/app/pages.test.ts`, `test/app/pagesRender.test.tsx`)
- [x] 2.2 Migrate Environments categories and application/library resource views; retain all operational capabilities without inner navigation tabs. (Inner table-tab row and its Tab/number keys removed; the feature takes its category/nested view from the route and reports its own destination changes back through `packages/devenv/cli/src/tui/destination-sync.ts` + the `destination` prop; every provider/issue/change-request/CI/utility view stays reachable; `test/app/environmentDestinations.test.tsx`, `test/app/appShellMount.test.tsx`)
- [x] 2.3 Migrate observability list/detail/span and Wiki navigation to the same route authority; preserve existing availability restrictions and wiki review drafts. (`src/tui/otel/app/App.tsx` derives every view from the route, `otel/app/navigation.ts` is modal-only, `WikiView` takes a route-owned `noteId`; `test/app/observabilityRoutes.test.tsx`, `test/otel/wikiNoteContext.test.tsx`)
- [x] 2.4 Make default/home/manager full-app entry open Home; keep explicit supported entry modes and the temporary workflow entry until its dependent changes land. (Home mode enters the `home` page; dash/attach entries keep their explicit destinations; Workflows stays a Home destination until contextual launch and Settings land; `test/app/appShell.test.tsx`)

## 3. Input and lifecycle
- [x] 3.1 Remove old shell/inner-tab cycling and numeric dispatch; resolve picker/Parent binding collisions and register page-local focus, Back and modal precedence once. (Shell rows, `t`/number dispatch and `TabBar` deleted; `Ctrl+P` picker and `Alt+Up` parent registered in `src/tui/dash/keymap-setup.ts`; `registerShellOverlayLayer` gives shell dialogs input precedence; Tab/Shift+Tab traverse page-local focus regions; `test/app/pageNavigation.test.tsx`. The imported environment body keeps its own inner table keys until task 2.2 migrates it.)
- [x] 3.2 Preserve selected IDs, filter/sort/search, scroll, focus and drafts across navigation; add unavailable-resource fallback without silent draft loss. (Route-keyed view state for destination cursors; trace selection follows `selectedTraceId`; a route→data effect loads the identity a route names; `resolveAvailable` replaces a vanished resource with its ancestor plus a diagnostic; `test/app/routeState.test.tsx`)
- [x] 3.3 Verify page visibility never changes backend/coordinator/telemetry ownership and foreground utility waiting remains asynchronous. (`test/app/pageOwnership.test.tsx`: coordinator identity and telemetry stores unchanged across page navigation; a pending foreground utility keeps waiting while input is processed)

## 4. Validation
- [x] 4.1 Add rendered navigation checks for Home → Applications → resource, Home → Observability → trace → span, cross-domain Back versus Parent, modal/text input isolation and inactive handlers. (`test/app/renderedNavigation.test.tsx`, `test/app/observabilityRoutes.test.tsx`, `test/app/appShell.test.tsx`; Parent is verified at the route level in `test/app/routes.test.ts` plus the catalog/binding checks in `test/app/pageNavigation.test.tsx` — the test input helper cannot emit Alt+Up)
- [x] 4.2 Open the TUI at narrow and wide sizes; check keyboard/mouse breadcrumbs, picker, every migrated destination, footer special keys and complete `?` help; record evidence. (Substituted with rendered frames: `test/app/tuiValidation.test.tsx` covers 48/200 columns, a real mouse click on an ancestor crumb, every migrated destination, the footer special keys and the complete `?` help; evidence recorded in `validation.md`.)
- [x] 4.3 Run relevant tests, `bun run type-check`, `bun run lint` with zero diagnostics, and strict OpenSpec validation; document the temporary workflow bridge and removal dependency. (`bun run lint` and `bun run type-check` are clean; `bun run test` and `bun run test:devenv` pass for every affected file — see `validation.md` for the two unrelated load-flaky workflow suites. The workflow page stays a Home destination until contextual launch and centralized Settings land; its removal dependency is recorded in `validation.md`.)
