# Tasks

## 1. Config schema, parser hard break, and migration

- [x] 1.1 In `agentic-coding/src/workflow/profiles.ts`, add `PoolEntry` (`label`, `profile`, `criteria?`, `default?`) and a `pools` table to `PresetConfig`/`RoutingPreset`, remove `PRESET_CATEGORY_KEYS`/`categoryProfile` and the flat category fields, and add the module constants (single confidence floor 0.5, roster threshold 0.2, roster min 2/max 5). Verify with `cd agentic-coding && bun run type-check`.
- [x] 1.2 Rework `parseAgentsConfig`/`validatePresets` to require pools, validate unique slug labels, known profiles, and the per-step default-count rule, and reject flat `easy|medium|hard|critical` keys and `roles["core.verification"]` with a provenance-file + Settings → Presets error. Verify with `bun test test/workflow-model-config.test.ts` and new parser cases.
- [x] 1.3 In `agentic-coding/src/config-migration.ts`, add the `strip-presets` migration action: `planMigration` detects `agents.presets` in the canonical JSON (parsing a pending legacy TOML first), `applyMigration` deletes the presets table while keeping profiles/default_profile/routes/role_routes/definition_defaults, reusing the backup/journal/staged-rename machinery, and the report states `Removed N presets; recreate them as model pools in Settings → Presets.`. Verify with new config-migration tests including a rerun/idempotency case.
- [x] 1.4 Add the missing-preset messaging: the `config migrate` output statement, the persistent TUI banner while the effective config has no custom presets, and the Settings → Presets hint on the hard start error; keep the built-in `use-default-model`. Verify via focused settings/migration tests and a TUI check.
- [x] 1.5 Delete the superseded `openspec/changes/add-complexity-model-routing-to-preset-editor/` and `openspec/changes/add-openspec-jev-apply-change-picker/` directories and confirm `openspec status` no longer lists them.

## 2. Settings preset editor

- [x] 2.1 In `agentic-coding/src/tui/settings/agentPresets.ts`, replace the fixed complexity fields and the nine verifier-role fields with per-classifiable-step `pool:<step>:labels` text fields plus one `pool:<step>:<label>` profile select per current label derived from the draft, plus the `fusion.plan` default toggle; update the draft, `applyDraftValue`, `draftValues`, and `presetMutation` to read/write pool entries, and extend `profileReferences` to scan pool entries. Do not extend the shared `Form` primitive. Verify with `bun test test/settings/agentPresets.test.ts`.
- [x] 2.2 Update `agentic-coding/src/tui/settings/AgentPresetsView.tsx` for the live per-label fields and the fusion 2–5 default enforcement on save, and change the Presets menu summary to `step and model-pool routing`. Verify with `bun test test/app/agentPresetsView.test.tsx` plus a TUI keybind/help check.
- [x] 2.3 Add tests that a label edit derives the matching profile selects, a save persists the pool entries, fusion default counts outside 2–5 are refused, and a profile referenced only by a pool entry cannot be deleted or renamed.

## 3. Classifier runner protocol

- [x] 3.1 In `agentic-coding/src/workflow/classifiers.ts`, widen `criteria` to JSON, add the `routing` integration whose questions are built per classifiable step mode and pool, make parsing mode-aware (single → `choice` + confidence gate; roster → probability threshold/de-dup/clamp), and keep the legacy `complexity` integration registered only so a claimed `{ integration: "complexity" }` payload can drain. Verify with `bun test test/workflow-classifiers.test.ts`.
- [x] 3.2 In `agentic-coding/src/workflow/classifier-runner.ts`, build one `questions` map per request holding every step question, and return the full answer (`{ type, choice, probabilities, confidence }` or `{ type: "noul" }`) instead of only the choice. Verify with runner/classifier tests covering multi-question requests and both answer shapes.

## 4. Steps, graphs, and catalog overhaul

- [x] 4.1 In `agentic-coding/src/workflow/steps/`, add the optional `classification?: "single" | "roster"` field to `StepBehavior`, add `core.route-plan`/`core.route-apply` behaviors that enqueue one `model.classify` with `{ integration: "routing", phase }` and transition `complete`, declare the per-step modes (`fusion.plan` roster; `core.plan`, `fusion.consolidate`, `core.implementation`, `core.triage`, `core.verification`, `core.wiki`, `core.archive` single), and delete `model-selection.ts`. Verify with `bun test test/workflow-steps.test.ts`.
- [x] 4.2 In `agentic-coding/src/workflow/definitions/steps.ts`, register `core.route-plan` and `core.route-apply` as system steps with `outcomes: ["complete"]`, `allowedEffects: ["model.classify"]`, `retryLimit: 3`, remove `core.model-selection`, and keep `behaviorVersion: 1`. Verify with `bun test test/workflow-registry.test.ts`.
- [x] 4.3 Rebuild the graph definitions: `graphs/openspec.ts` registers `openspec` (route-plan → plan → plan-approval → route-apply → implementation loop → tail), `openspec-apply` (route-apply → implementation loop → tail), and `openspec-propose` (route-plan → plan → plan-approval → completed → closed); `graphs/fusion.ts` registers `openspec-fusion` (route-plan → fusion.plan → fusion.consolidate → plan-approval → loop → tail) and `openspec-fusion-propose` (route-plan → fusion.plan → fusion.consolidate → plan-approval → completed → closed); fold `openspec-jev.ts` into `openspec.ts` and unregister `openspec-full`, the old `openspec-apply`, `openspec-fusion-full`, the old `openspec-fusion-propose`, `openspec-jev`, and `openspec-jev-apply`. Verify with `bun test test/workflow-registry.test.ts` and catalog tests.
- [x] 4.4 Update `definitions/catalog.ts` and any start/status/drain path so an unknown or removed definition id produces an `unknown/removed definition` diagnostic naming the id and a registered alternative instead of a generic pin error. Verify with `bun test test/workflow-registry.test.ts` and a focused removed-id start case.
- [x] 4.5 Remove `--fusion-profiles`: delete the flag from `cli/commands/start.ts`, `cli/schema.ts`, and `cli/help.ts`, and remove `parseFusionProfiles`/`fusionProfiles` handling from `startup.ts` so fusion planner routing comes only from the `fusion.plan` pool. Verify with `bun test test/workflow-startup.test.ts test/workflow-plan-fusion.test.ts`.

## 5. Routing reducer and startup

- [x] 5.1 In `agentic-coding/src/workflow/runtime/reducers/effect-result.ts`, make `applyClassifierRouting` phase-aware (select the `plan` or `apply` pool set from the payload), apply every single selection, and change `resolveRouting` in `profiles.ts` to replace **every** route of a selected step so one `core.verification` pool covers all verifier roles. Verify with `bun test test/workflow-classifiers.test.ts test/workflow-steps.test.ts`.
- [x] 5.2 Recompute the fusion roster in the reducer: `rolesForDefinition(..., roster.length, ...)`, override planner-K profiles, then `validateFusionRouting`, preflight each profile, and `enforceReadOnlySteps`; on any failure keep the tagged default, record `attention`, and never strand the run. Verify with fusion routing tests.
- [x] 5.3 Rework `validatePresetCoverage` in `profiles.ts` to require a valid pool for every classifiable step in the resolved definition, call it from both `startup.ts`'s start path and the `switch-preset` reducer (fixing the missing switch-preset call), and fail a classifier-routed start without a preset with the Settings → Presets hint. Verify with `bun test test/workflow-startup.test.ts` and a switch-preset coverage case.

## 6. Documentation, cleanup, and validation

- [x] 6.1 Update `agentic-coding/docs/workflow-architecture.md`, `README.md`, and the config/settings inventories for the new workflow ids, routing steps, pools, and the removed `--fusion-profiles` option; delete the legacy `complexity` integration from `classifiers.ts` (and its `effect-runner.ts` drain branch) now that the cutover is merged. Verify by `rg` that no doc or source references the old flat keys, removed workflow ids, or `--fusion-profiles`.
- [x] 6.2 From `agentic-coding/`, run the focused suites touched by this change (`test/workflow-classifiers.test.ts`, `test/workflow-steps.test.ts`, `test/workflow-registry.test.ts`, `test/workflow-model-config.test.ts`, `test/settings/agentPresets.test.ts`, `test/app/agentPresetsView.test.tsx`, `test/workflow-startup.test.ts`, `test/workflow-plan-fusion.test.ts`, config-migration and catalog tests) with zero failures.
- [x] 6.3 From `agentic-coding/`, run `bun run lint`, `bun run type-check`, and `bun run build` with zero diagnostics, confirming `src/workflow/embedded.generated.ts` was regenerated by `bun run build` and never hand-edited.
- [x] 6.4 From `agentic-coding/`, run `bun test test/workflow-source-layer-boundaries.test.ts test/workflow-module-import-cycles.test.ts test/workflow-module-exports.test.ts` with zero failures.
