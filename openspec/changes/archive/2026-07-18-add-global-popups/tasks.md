## 1. Configure popup keybindings

- [x] 1.1 Edit `~/.config/herdr/config.toml`: rebind `goto` to `prefix+shift+g`, `edit_scrollback` to `prefix+shift+e`.
- [x] 1.2 Edit `~/.config/herdr/config.toml`: add `[[keys.command]]` entries for lazygit popup on `prefix+g` and nvim popup on `prefix+e`.
- [x] 1.3 Validate with `herdr config check` and reload config via `herdr server reload-config`.
- [x] 1.4 Test each popup opens and closes correctly.
