## 1. Configuration and bundle module

- [x] 1.1 Confirm the `Bun.YAML` version floor: check whether `Bun.YAML.parse` exists on the `packageManager` pin in `agentic-coding/package.json` (currently `bun@1.3.14`; verified working on 1.4.0) and raise the pin if it does not.
- [x] 1.2 Add an optional `wiki?: { root?: string }` section to `WorkflowConfig` in `agentic-coding/src/workflow/effects.ts` and `wiki: { root: "~/.config/agentic-coding/wiki" }` to `DEFAULT_CONFIG`, keeping the field optional so existing config fixtures still type-check.
- [x] 1.3 Create `agentic-coding/src/workflow/wiki.ts` with `wikiRoot()` resolving `HERDR_WIKI_DIR` → `loadConfig().wiki?.root` (tilde-expanded) → `~/.config/agentic-coding/wiki`, and `ensureBundle()` creating the root and a root `index.md` whose frontmatter carries `okf_version: "0.2"`, idempotently. Do not create a manifest file — OKF v0.2 has none.
- [x] 1.4 Add `conceptPath(rel)` validating that the resolved path stays inside the bundle root, rejecting `..` and absolute escapes, and rejecting the reserved filenames `index.md` and `log.md` as concept documents.
- [x] 1.5 Add `parseDocument(text)` splitting the `---`-delimited frontmatter from the body and parsing the frontmatter with `Bun.YAML.parse`, so nested mappings (`generated: { by, at }`) and lists of mappings (`sources`, `verified`) all parse. No third-party YAML dependency.
- [x] 1.6 Add `renderDocument(frontmatter, body)` emitting **block-style** frontmatter — one top-level field per line — covering the shapes this producer writes (scalars, string lists, inline `{ by, at }` maps, and the `sources` list of maps), and re-emitting unrecognized keys read from an existing document. Do not use `Bun.YAML.stringify`: it emits flow style on a single line and destroys line-level diffs.
- [x] 1.7 Add `checkConformance(doc)` implementing OKF §11 for the consumer side: reject only unparseable frontmatter or an absent/empty `type`. Never reject for missing optional fields, unknown `type` values, unknown extra keys, broken links, or a missing `index.md`.
- [x] 1.8 Add `validateProducerFields(input)` for the producer side: require `type`, `title`, and `description`; validate `status` against `draft|stable|deprecated`; validate timestamps as ISO 8601 with an explicit UTC offset; validate actor strings against the `<producer>/<version>` / `human:<id>` / `process:<id>` conventions; require a `resource` on every `sources` entry.
- [x] 1.9 Add `trustTier(doc)` returning unverified (no `verified`), machine-confirmed (non-`human:` actors only), or human-reviewed (any `human:` actor), treating a bare `verified` mapping as a one-element list. Add `effectiveStatus(doc)` defaulting an absent `status` to `stable` and `isStale(doc, now)` returning `now >= stale_after`.
- [x] 1.10 Add `listConcepts({ tag, type })` synthesizing the directory listing at read time from the concept documents themselves — do not read or maintain a stored `index.md` below the bundle root — and `readConcept(id)`.
- [x] 1.11 Add `searchConcepts(terms, limit)` doing a bounded linear scan with scoring (title 4, tags 3, headings 2, body 1) and returning per hit the concept id, title, tags, `status`, trust tier, staleness, and a snippet.
- [x] 1.12 Add `writeConcept(path, input)` that validates producer fields, sets `generated: { by, at }`, merges into an existing document in place when one exists (preserving unknown keys), snapshots the prior state on first touch (task 6.1), and installs by temp-file-plus-rename in the target directory so a failed write leaves nothing behind.
- [x] 1.13 Add `verifyConcept(path, actor)` appending a `verified` event and setting `status: stable`, idempotent across repeated runs, and `appendLog(dir, entry)` inserting under an ISO `YYYY-MM-DD` heading in `log.md`, newest date first, per OKF §9.

## 2. CLI surface

- [x] 2.1 Add `"wiki"` to `SUBCOMMANDS` and a `WIKI_SUBCOMMANDS = ["list", "search", "show", "write", "verify", "log"]` const in `agentic-coding/src/workflow/cli.ts`, mirroring the `AGENT_EXTENSION_SUBCOMMANDS` pattern.
- [x] 2.2 Add the `command === "wiki"` dispatch branch: reject an unknown operation with a non-zero exit and usage; wire `list` (`--tag`, `--type`, `--json`), `search` (terms, `--limit`, `--json`), and `show <id>`; require no repository, change, or workflow state for any wiki operation.
- [x] 2.3 Wire `write --path <p> --type <T> --title <T> --description <D> [--tags --resource --status --stale-after --source <resource> --body-file <f>]`, rejecting missing required arguments with usage.
- [x] 2.4 Gate `write`: allow when `HERDR_ROLE` is unset or is the planner, consolidator, or archive role; reject every other role (including the parallel fusion-planner roles, worker, triage, and verifiers) with a non-zero exit. For non-archive roles force `status: draft`, set `generated.by` to an actor derived from the role and profile, and refuse or strip any `verified`/`status: stable` request.
- [x] 2.5 Wire `verify --path <p> [--actor <a>]` and `log --entry <text> [--path <dir>]`, both gated to `HERDR_ROLE` unset or `archive`, with `verify` defaulting to `process:herdr-archive` as the verifying actor.
- [x] 2.6 Reject any `human:`-prefixed actor whenever `HERDR_ROLE` is set, on both `write` and `verify`, with an error explaining that the human-reviewed tier is granted only by the approval gate. The engine's `wiki.verify` effect (task 6.3) calls the module directly and is not subject to this CLI gate.
- [x] 2.7 Add the `wiki` usage lines to `help()` describing the six operations, the draft-vs-verify split, and which roles may write.

## 3. Agent instructions

- [x] 3.1 Update `agent-definitions/instructions/planning.md`: consult the bundle with `agentic-coding workflow wiki search`/`show` before deciding; state that wiki access is exempt from the existing "do not explore" restriction; weight each concept by `status`, trust tier, and staleness rather than treating it as authoritative; and permit writing a draft concept with `wiki write` when knowledge is worth capturing, noting that archive is what promotes a draft to verified.
- [x] 3.2 Update `agent-definitions/instructions/planning-fusion.md` with the same read and trust-weighting guidance, exempt wiki reads from the "read only the files the change touches" restriction, keep the no-coordination rule intact, and **explicitly forbid writing** because these planners run in parallel — gaps go in this draft's own `risks`/`questions`.
- [x] 3.3 Update `agent-definitions/instructions/fusion-consolidation.md`: reconcile draft claims against the bundle, prefer a verified stable concept over a draft that contradicts it and record the reason in the design notes, and permit writing draft concepts for gaps the planners surfaced (this role is sequential).
- [x] 3.4 Update `agent-definitions/instructions/archive.md`: after a successful `openspec archive`, promote the drafts written during this change with `agentic-coding workflow wiki verify`, write or update any remaining durable knowledge with `wiki write`, mark superseded concepts `status: deprecated` instead of deleting them, append a `wiki log` entry for the change, relax the "do not explore beyond the change directory" sentence to permit wiki access, list touched concept ids in the archive evidence, state explicitly when nothing was worth recording, and report a wiki failure in the evidence rather than as a blocked outcome.
- [x] 3.5 Add a "Revising after wiki review" section to `archive.md`, mirroring the existing "Revising after plan review" section in `planning.md`: read the gate's comments from `agentic-coding workflow status --json` (`step.context.comments`, each with `comment`, `file`, and optional line anchors), apply each to the concept it names, record which comment each edit resolves, say so explicitly when a comment is invalid or already satisfied, and do not repeat `openspec archive` on a change that is already archived.
- [x] 3.6 Verify no wiki text was added to `workflow-agent-protocol.md`, `implementation.md`, `triage.md`, or any `verification*.md`, and keep each instruction addition to a short paragraph so assignments stay under `MAX_ASSIGNMENT_BYTES`.
- [x] 3.7 Regenerate the embedded bundle with `cd agentic-coding && bun run build` so `embedded.generated.ts` matches the edited instructions. Never hand-edit that file.

## 4. Tests

- [x] 4.1 Create `agentic-coding/test/workflow-wiki.test.ts` covering, against a temporary `HERDR_WIKI_DIR`: root resolution precedence (env > config > default with tilde expansion); `ensureBundle` creates a root `index.md` carrying `okf_version: "0.2"`, is idempotent, and writes no manifest file.
- [x] 4.2 Add OKF §11 consumer-permissiveness coverage: a concept with only `type` is accepted; an unknown `type` value is accepted; unknown extra frontmatter keys are accepted and survive a read-write round trip; a broken cross-link does not fail a read; a missing `type` and unparseable frontmatter are the only rejections.
- [x] 4.3 Add format coverage: `generated: { by, at }`, a `verified` list of mappings, and a `sources` list with credibility signals all parse; a bare `verified` mapping is treated as a one-element list; rendered frontmatter is block style with one top-level field per line, not a single flow-style line.
- [x] 4.4 Add path-safety coverage: `conceptPath` rejects `..`, absolute escapes, and the reserved `index.md` / `log.md` filenames.
- [x] 4.5 Add trust and lifecycle coverage: `trustTier` returns unverified / machine-confirmed / human-reviewed for the three cases; absent `status` reports `stable`; an invalid `status` is rejected on write; `isStale` is true once `stale_after` has passed.
- [x] 4.6 Add read coverage: `listConcepts` synthesizes a listing with no stored index below the root and filters by tag; `searchConcepts` ranks a title hit above a body hit, honours the limit, and reports status, trust tier, and staleness per hit.
- [x] 4.7 Add write coverage: a rejected producer-field validation leaves no partial file; writing an existing path updates it in place and bumps `generated.at`; provenance records the originating change with a `resource` on every `sources` entry; `verifyConcept` is idempotent across repeated runs; `appendLog` groups by ISO date heading newest first.
- [x] 4.8 Add CLI role-gate coverage: `write` succeeds with `HERDR_ROLE` unset and for the planner, consolidator, and archive roles; it forces `status: draft` and writes no `verified` for planner and consolidator, and refuses their attempts to set `status: stable` or supply `verified`; it exits non-zero for the parallel fusion-planner roles, `worker`, and a verifier role; `verify` and `log` succeed only for archive or an unset role; a `human:`-prefixed actor is refused on both `write` and `verify` whenever `HERDR_ROLE` is set; `list`/`search`/`show` succeed with `HERDR_ROLE` unset.
- [x] 4.9 Extend `agentic-coding/test/workflow-cli.test.ts` so the table-driven `SUBCOMMANDS` assertions include `wiki`, and cover an unknown wiki operation and a missing required argument both exiting non-zero with usage.
- [x] 4.10 Extend `agentic-coding/test/workflow-assets.test.ts` to assert each of the four edited instructions has an embedded sha256 equal to its on-disk file, and that no wiki reference appears in the instruction assets pinned to `core.implementation`, `core.triage`, or `core.verification`.

## 5. Documentation and validation

- [x] 5.1 Add a "Knowledge wiki" section to `README.md`: that the bundle is Open Knowledge Format v0.2 (linking `github.com/GoogleCloudPlatform/open-knowledge-format`), where it lives and how to relocate it (`HERDR_WIKI_DIR` / `[wiki] root`), the concept document shape and reserved filenames, the six `agentic-coding workflow wiki` operations, the draft-then-verify trust model and which roles may write, the wiki approval gate and how approval grants the human-reviewed tier, the `[wiki] reviewer` key, the note that OKF §3 recommends `git init` on the bundle for history, and that changing pinned instructions or the manifests may require `agentic-coding workflow repin` for in-flight workflows.
- [x] 5.2 Add a `[wiki]` section with `root = "~/.config/agentic-coding/wiki"` to `pi/herdr-workflow.toml` so the setting is discoverable and matches `DEFAULT_CONFIG`.
- [x] 5.3 Run the focused checks for this change: `cd agentic-coding && bun test test/workflow-wiki.test.ts test/workflow-wiki-gate.test.ts test/workflow-cli.test.ts test/workflow-assets.test.ts test/workflow-registry.test.ts test/workflow-dashboard.test.ts`, plus `bun run type-check` and `bun run lint` (zero diagnostics; tabs, no non-null assertions).

## 6. Wiki approval gate

- [x] 6.1 Add `snapshotOnFirstTouch(changeId, conceptPath)` to `agentic-coding/src/workflow/wiki.ts`, copying the prior document (or writing a tombstone when the concept is new) to `.herdr-workflow/<changeId>/wiki-snapshot/<path>.md` only when no snapshot entry exists yet, plus `snapshotList(changeId)` returning the touched-concept list and `snapshotRead(changeId, conceptPath)` returning the pre-change content for diffing.
- [x] 6.2 Add the `core.wiki-approval` developer step to `definitions.ts` with outcomes `approve` and `comments`, and add `"wiki.verify"` to the `EFFECTS` list with the step's `allowedEffects` covering it.
- [x] 6.3 Implement the `wiki.verify` effect in `agentic-coding/src/workflow/effect-runner.ts`: resolve the reviewer as `[wiki] reviewer` → `git config user.email` → `human:developer`, then append `verified: { by: human:<id>, at }` to every concept in the change's snapshot list by calling the wiki module directly. Make it idempotent so a retried effect does not duplicate verification events.
- [x] 6.4 Wire the edges in the `archive` branch of `workflowEdges(...)`: retarget `core.archive` `complete` to `core.wiki-approval`; add `core.wiki-approval` `approve` → `core.delivery` carrying the `wiki.verify` effect, and `comments` → `core.archive` with `loop: { maxAttempts: 6 }`. Insert `"core.wiki-approval"` between `core.archive` and `core.delivery` in the `standard`, `direct-apply`, and `plan-fusion` manifest `steps` arrays. Leave `no-openspec` untouched.
- [x] 6.5 Add the `core.wiki-approval` branch to the gate-actions switch in `agentic-coding/src/workflow/runtime.ts`, offering `approve-wiki` (confirm) and `review-comments` (`core.review-comments@1`), reusing the existing bounded-comment validation unchanged.
- [x] 6.6 Add `saveWikiReview`/`loadWikiReviewComments` to `agentic-coding/src/tui/dash/data.ts` writing `reviews/wiki-review.json`, mirroring the `plan-review.json` helpers, and add a trigger-only `wiki-review` key to `requiredUserActionFor` for the `core.wiki-approval` phase.
- [x] 6.7 Build the wiki review modal: a concept-list popup sourced from `snapshotList` with per-concept change counts (reusing `ChangedFilesBrowser`), opening a selected concept in a diff of snapshot-versus-current with line-anchored comments (reusing the developer-review diff modal and `MarkdownViewModal` comment anchoring), plus finish and postpone handling that dispatches approve or comments. Wire the direct-open trigger in `App.tsx`.
- [x] 6.8 Create `agentic-coding/test/workflow-wiki-gate.test.ts` covering snapshot semantics: first touch captures prior content, a new concept records a tombstone, a second write in the same change preserves the original snapshot, and `snapshotList` enumerates exactly the touched concepts.
- [x] 6.9 Extend that file with routing coverage: every archive-bearing manifest places `core.wiki-approval` between `core.archive` and `core.delivery`; `no-openspec` has no gate; `approve` reaches delivery and `comments` returns to `core.archive` under the bounded loop; and no step reachable before the gate resolves declares `workspace.close` or `workspace.cleanup`.
- [x] 6.10 Extend it with promotion coverage: approval appends a `human:` verification to every snapshot-listed concept and reports the human-reviewed tier; untouched concepts are unchanged; a comments outcome promotes nothing; a repeated effect does not duplicate verification events; reviewer identity falls back through config, git, and the generic actor.
- [x] 6.11 Add dashboard coverage in `agentic-coding/test/workflow-dashboard.test.ts`: `requiredUserActionFor("core.wiki-approval")` returns the stable `wiki-review` trigger key with no selectable items, and the wiki review comments round-trip through `reviews/wiki-review.json`.
