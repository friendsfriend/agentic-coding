## REMOVED Requirements

### Requirement: Overview displays structured Git status
**Reason**: The workflow overview/list is intentionally removed; Herdr owns workspace access.
**Migration**: Use existing application/library repository views for Git state. Do not add a project-local workflow list or remove shared Git providers used by other consumers.

### Requirement: Git status inspection is safe and consistent
**Reason**: The workflow overview/list is intentionally removed; Herdr owns workspace access.
**Migration**: Use existing application/library repository views for Git state. Do not add a project-local workflow list or remove shared Git providers used by other consumers.

### Requirement: Overview renders git status as a single compact line
**Reason**: The workflow overview/list is intentionally removed; Herdr owns workspace access.
**Migration**: Use existing application/library repository views for Git state. Do not add a project-local workflow list or remove shared Git providers used by other consumers.
