## 1. Establish the cleanup baseline

- [ ] 1.1 Refresh the test inventory and inspect current working-tree changes; group files by the audit's functional clusters without moving them or modifying concurrent work.
- [ ] 1.2 Run `bun run test` and `bun run test:devenv` from `agentic-coding/`; create change-local `verification.md` recording Bun version, commands, counts, elapsed time, skips, and failures. Recheck the reported process-tree timeout if it recurs without weakening its assertion.
- [ ] 1.3 Record each proposed deletion's guarantee and surviving detector, or its test-local/incidental-only rationale. Check existing specifications and callers; retain ambiguous and still-required export, asset, instruction, and boundary checks.

## 2. Compact historical compatibility expectations

- [ ] 2.1 Mechanically deduplicate the existing literal definition/step expectations in `test/workflow-steps.test.ts`, preserving every previously asserted tuple and the existing historical filter. Do not regenerate expected values from current registry output.
- [ ] 2.2 Prove the compact fixture reconstructs the complete original expectation before removing the repeated block; record the equality check and before/after total fixture-plus-test size.
- [ ] 2.3 Run the focused compatibility suites and isolated missing-identity, changed-digest, and changed-step-association probes; confirm each mutation fails and restored code passes.

## 3. Replace false-confidence behavioral checks

- [ ] 3.1 Replace manufactured route-array pushes in `test/app/pagesRender.test.tsx` with real production input assertions or remove them where existing journey tests cover the identical path. Retain unique rendering checks and verify actual picker acceptance and breadcrumb navigation.
- [ ] 3.2 Add terminal-relative positioning coverage using the production modal beneath offset shell content in an existing modal suite; remove `test/dash/modalCentering.test.ts` only after its intended guarantee has that detector. Preserve stacking and lifecycle checks.
- [ ] 3.3 Correct `otel/topologyStore.test.ts` to exercise a genuine cycle if retaining its cycle claim; rename nullish cases in `otel/otlp-grpc.test.ts` and verify actual payload-size protection remains covered at its owning boundary.
- [ ] 3.4 Re-run isolated picker-callback and modal-Portal faults against the repaired checks; record passing baselines, expected assertion failures, and restored passes in `verification.md`.

## 4. Repair and consolidate architecture guards

- [ ] 4.1 Replace the environment ownership regex with resolved edges from the existing source-graph helper; preserve the intended workflow/telemetry restriction and allowed composition-root access.
- [ ] 4.2 Add small positive/negative fixtures covering ordinary named imports and supported alternate dependency forms; confirm a forbidden workflow-to-environment-state import now fails with source/target evidence.
- [ ] 4.3 Consolidate only duplicate cycle scans and source checks whose constraints are demonstrably preserved; retain the distinct parent-barrel rule, layer/purity checks, and real cross-database isolation test.

## 5. Remove proven redundancy

- [ ] 5.1 Remove test-local `shape` and `caseNamed` checks and redundant repeated deterministic calls, preserving assertions on production discovery, parsing, and projections.
- [ ] 5.2 Review `otel/tabShell.test.ts` against store and live-refresh coverage; remove redundant constructor/method-existence checks while retaining any unique empty-state guarantee in its owning suite.
- [ ] 5.3 Remove only mapped redundant source/asset/prose assertions after verifying surviving packaging, executable bridge, and required instruction-policy coverage. Do not blanket-delete historical/internal-export or cosmetic suites.
- [ ] 5.4 Confirm live trace refresh still rejects empty summaries and server authorization still rejects forged tokens; record these focused fault probes as positive controls for the retained suite.

## 6. Make runtime smoke checks truthful

- [ ] 6.1 Replace successful early returns for unselected runtimes or unavailable prerequisites with supported Bun skip reporting; keep explicit opt-in and bounded preflight, and fail unexpected operations after prerequisites succeed.
- [ ] 6.2 Replace or combine weak foreign-resource assertions with before/after checks using uniquely test-owned separate-owner sentinels. Keep teardown bounded and restricted to fixture-owned resources; never modify unrelated workloads.
- [ ] 6.3 Ensure focused and aggregate reporting distinguish executed passes, failures, and skipped cases; minimally adjust the existing runner only if necessary, retaining process isolation, watchdogs, and non-zero failure exits.
- [ ] 6.4 Verify default/missing-prerequisite skip paths and attempted-operation failure reporting. Run live smoke only with explicitly opted-in available runtimes; otherwise record it as unverified/skipped, not passed.

## 7. Verify and prepare the review

- [ ] 7.1 Run all changed focused suites, then both complete configured Bun suites and any affected shell checks; account for all remaining failures and count differences without deleting failing contracts or claiming unexplained failures are resolved.
- [ ] 7.2 Run `bun run lint` with zero diagnostics, `bun run type-check`, and `bun run build` from `agentic-coding/`; review generated output without hand-editing it and preserve unrelated working-tree changes.
- [ ] 7.3 Run `openspec validate cleanup-tests-without-coverage-loss --strict`; finalize `verification.md` with the removal-to-detector mapping, exact pin-equivalence evidence, fault-probe results, total size/count changes, and remaining uncertainty.
- [ ] 7.4 Present cleanup results for discussion. Leave broad cosmetic pruning and final `AGENTS.md` testing guidelines for the next explicitly approved change.
