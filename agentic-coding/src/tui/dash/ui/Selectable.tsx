/** @jsxImportSource @opentui/solid */
// Dashboard entry point for the shared selection primitives. The list is
// windowed (`ScrollableList`) rather than a scroll box, so there is no scrollbar
// surface to theme here any more.
export {
	Selectable,
	SelectableList,
	type SelectableListProps,
} from "../../shared/Selectable";
