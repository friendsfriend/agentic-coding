/** @jsxImportSource @opentui/solid */
// Observability entry point for the shared selection primitives. The list is
// windowed (`ScrollableList`) rather than a scroll box, so it has no focus or
// scrollbar options to pin any more.
export {
	Selectable,
	SelectableList,
	type SelectableListProps,
} from "../../shared/Selectable";
