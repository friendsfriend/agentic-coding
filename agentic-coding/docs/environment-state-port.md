# Environment state and configuration ownership

`port-project-catalog-and-state-to-bun` moves the configured-environment
authority (definition files, project catalog, `$DEVENV_HOME/db/state.db`) from
the Go backend into `src/server/environment/`. Until the cutover completes, Go
keeps its existing behavior and Bun consumes the same on-disk data.

## Predecessor

`expose-unified-bun-backend` is implemented (archived, all tasks checked):
`src/server/` owns workflow/telemetry and delegates `/api/v1/environment/*` to
the private Go child, which is why this change can port the environment domain
behind an unchanged route contract.

## Writer inventory (Go, non-test)

Every location that today writes environment state or configuration. This is
the list that must be quiesced at cutover (task 3.4) and re-pointed (task 3.2);
`test` files and read-only paths are excluded.

| Writer | Location | Owns |
| --- | --- | --- |
| Schema authority | `pkg/state/store.go` (`Open`, `migrate`, `applyV1`-`applyV7`) | Creates/migrates `state.db`; every state write below routes through this `Store`. |
| Store container | `pkg/services/container.go` (`state.Open(dbDir)`) | Single process-wide `StateStore` instance and its lifetime. |
| Definition-file writer | `pkg/app/manager.go` (`AddApp`, `RemoveApp`, `SaveConfig`, `saveAppFile`, `saveInfraServicesFile`) | `apps/definitions/*.json`, `libraries/definitions/*.json`, `infrastructure/definitions/*.json`. |
| Runtime-state writers | `pkg/app/manager.go` (`SetBranch`, `SetAppState`, `SetMainWorktreeBranch`, `loadRuntimeState` backfill, `updateBranches`) | `app_state.branch`, `active_worktree`, `main_worktree_branch`. |
| Run-target state | `pkg/build/service.go` (`SetAppRunTargetInfo`, `ClearAppRunTargetInfo`) | `app_state.run_target_*`. |
| Dependency leases | `pkg/server/dependency_leases.go` (`SetDependencyLease`, `DeleteDependencyLease`) | `dependency_leases`. |
| Action history | `pkg/server/server.go` (`AddActionEvent`, `AddActionLogEvent`) | `action_events`, `action_log_events`. |
| Script argument history | `pkg/server/handlers_scripts.go` (`AddScriptArgsHistory`) | `script_args_history`. |
| Post-clone branch | `pkg/server/handlers_apps.go`, `pkg/server/server.go` (`AppManager().SetMainWorktreeBranch`) | `app_state.main_worktree_branch`. |
| Read-only catalog | `cmd/catalog.go` (`state.OpenReadOnly`) | None — must stay read-only and schema-complete. |

Not in this slice: script/shell-action/endpoint/kubernetes-cluster definition
writers (`pkg/resources/{scripts,shell_actions,endpoints,kubernetes_config}.go`,
`pkg/exampleconfig/generator.go`, `pkg/server/handlers_scripts.go` create/link/
delete) ship with the scripts/actions port and keep their current owner.

## Captured fixtures (task 1.2, 1.3)

Generated by `server/pkg/state/fixtures_test.go`
(`DEVENV_ENV_FIXTURE_DIR=... go test ./pkg/state -run TestWriteEnvironmentFixtures`)
and asserted by `test/environment-state.test.ts` / `test/environment-config.test.ts`:

| Fixture | Content |
| --- | --- |
| `state/v1`, `state/v3`, `state/v5`, `state/v6` | Go-created database at each supported schema version with representative rows. |
| `state/current` | A v6 database opened by the production Go migration, i.e. the real v7 output. |
| `state/partial-v4` | Interrupted upgrade: v4 columns added, version row still 3. |
| `state/future` | Version 99 — must fail closed and stay byte-identical. |
| `config/principal` | App/library/infrastructure definitions (including runtime fields a definition file must never gain), `${CONFIG}`/`$CONFIG` placeholders and a `.env` with `$HOME` expansion. |
| `config/duplicate-ident`, `config/invalid-infra`, `config/unparseable-app` | Failure fixtures for duplicate idents, invalid service normalization and unparseable JSON. |
| `config/worktree-apps` + `home/layout.json` | Active-worktree resolution: linked worktree present, linked worktree removed, primary selected explicitly, never cloned. Materialized into a temporary directory because a checked-in `.git` directory would be an embedded repository. |

Action events expire after 24 hours, so tests shift the captured timestamps
forward at read time while preserving the relative offsets the ordering and
cursor contracts depend on (`ageEventTimestampsToNow`).

## Bun ownership (implemented)

- `src/server/environment/config.ts` — pure model: definition parsing,
  infrastructure normalization, `$CONFIG` expansion, project-catalog
  projection/revision (Go-compatible field order and `omitempty` behavior) and
  active-checkout resolution.
- `src/server/environment/catalog-observer.ts` — bounded read-only filesystem
  and Git boundary for the projection.
- `src/server/environment/state-store.ts` — `bun:sqlite` store with the
  `BEGIN IMMEDIATE` migration transaction, version re-read under the write lock,
  fail-closed future schema, `VACUUM INTO` pre-upgrade backup and
  post-migration integrity check.
- `src/server/environment/manager.ts` — definition loading/writing, runtime
  state overlay, explicit initialization backfill and branch refresh.
- `src/server/environment/authority.ts` — composition root plus the ownership
  switch (`DEVENV_ENVIRONMENT_OWNER`, default `bun`; `go` rolls back to the
  previous single owner).
- `src/server/environment/private-api.ts` — the bounded private operation
  envelope (task 3.1): one logical operation per request, decoded against a
  closed literal set, no SQL/table/column on the wire, served in-process.

## Mixed-runtime cutover (implemented)

- `POST /api/v1/environment/private/state` is served by Bun and reached by the
  Go child through `server/pkg/environment` (`Client.Store()` /
  `Client.Manager()`), which implement the existing `state.Store` and
  `app.Manager` interfaces. No Go call site changed.
- `pkg/services/openEnvironment` selects the owner: with
  `DEVENV_ENVIRONMENT_URL` set, the Go process opens **no** database handle and
  parses no configuration; without it, the previous owner is unchanged.
- Startup order is Bun → Go in migrated mode (headless server and TUI), so the
  Bun authority exists before the child that calls it; the delegation target is
  resolved per request, which is what makes the order possible.
- Go's `LoadConfig`/`LoadCatalogConfig` delegate to Bun and refresh the Go
  snapshot from the same response, so a failed reload keeps the last good
  catalog published.

## Rollback gate

- Roll back by stopping the stack and setting `DEVENV_ENVIRONMENT_OWNER=go`;
  never run both generations as writers.
- An upgrade from an older schema leaves `state.db.backup-v<N>` (consistent
  `VACUUM INTO`, written once per source version).
- A newer schema fails closed without modification and without a backup.
- An interrupted upgrade is either completed idempotently (missing columns are
  re-added) or rolled back as a whole; there is no partial migration.

## Bridge removal inventory (for `retire-go-backend-and-migration-bridges`)

Remove in the final cleanup, after the remaining Go environment routes are
ported:

- `server/pkg/environment/` (client, store and manager adapters) and the
  `openEnvironment` migrated branch in `server/pkg/services/container.go`.
- The `environment` option of `startOwnedBackend` plus the
  `DEVENV_ENVIRONMENT_URL`/`DEVENV_ENVIRONMENT_TOKEN` child variables, and the
  `environmentBaseUrl`/`environmentToken` delegation in `src/server/app.ts`
  (which is the only reason the lazy resolvers exist).
- The `DEVENV_ENVIRONMENT_OWNER` switch itself once the Go child is gone.

## Remaining in this change

None. Tasks 4.2/4.3 verified that the workflow and telemetry stores stay
untouched by the environment authority and that only
`src/server/environment/state-store.ts` opens the environment database; the
interactive catalog/history/action journeys are covered by the dashboard suites
plus the cross-runtime cutover test, not by a headless renderer journey.
