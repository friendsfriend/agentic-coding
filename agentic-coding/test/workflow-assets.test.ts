import { expect, test } from 'bun:test';
import { AGENT_DEFINITIONS } from '../src/workflow/embedded.generated.ts';

test('embedded workflow assets stay outside skill/plugin discovery', () => { const names = Object.keys(AGENT_DEFINITIONS); expect(names.some(name => name.includes('SKILL.md'))).toBe(false); expect(names.every(name => name.startsWith('instructions/') || name.startsWith('bridges/'))).toBe(true); expect(names).toContain('bridges/pi-telemetry.ts'); expect(names).toContain('bridges/opencode-telemetry.js'); expect(AGENT_DEFINITIONS['bridges/pi-telemetry.ts']).toContain("name: 'herdr_handoff'"); for (const bridge of ['bridges/pi-telemetry.ts', 'bridges/opencode-telemetry.js', 'bridges/opencode-v2-telemetry.js']) { expect(AGENT_DEFINITIONS[bridge]).toContain('herdr_check'); expect(AGENT_DEFINITIONS[bridge]).toContain('bwrap'); expect(AGENT_DEFINITIONS[bridge]).toContain('unshare-all'); expect(AGENT_DEFINITIONS[bridge]).toContain('script'); expect(AGENT_DEFINITIONS[bridge]).toContain('no-drain') } expect(names).toContain('bridges/opencode-v2-telemetry.js'); for (const name of ['bridges/pi-telemetry.ts', 'bridges/opencode-telemetry.js', 'bridges/opencode-v2-telemetry.js']) { const source = AGENT_DEFINITIONS[name]!; expect(source).toContain('schemaVersion: 1'); expect(source).toContain('traceparent'); expect(source).not.toMatch(/herdr\.db|state\.json|nudge|switch.*runtime/i) } });
