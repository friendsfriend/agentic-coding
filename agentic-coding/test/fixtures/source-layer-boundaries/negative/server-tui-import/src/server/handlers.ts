// Negative fixture: the server transport reaching into TUI presentation.
import type { WorkflowState } from "../tui/dash/state.ts";
import { Card } from "../tui/shared/Card.tsx";

export const state: WorkflowState | undefined = undefined;
export const card = Card;
